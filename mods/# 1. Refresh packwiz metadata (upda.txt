# 1. Refresh packwiz metadata (updates index.toml hashes after your .pw.toml edits)
packwiz refresh

# 2. Confirm remote is still set correctly (only needed if you're unsure / got the "already exists" error before)
git remote -v
# If it's missing or wrong:
# git remote add origin https://github.com/yourusername/your-repo-name.git
# git remote set-url origin https://github.com/yourusername/your-repo-name.git

# 3. Stage and commit changes
git add .
git commit -m "Fix Macaw's mod version pinning + other metadata fixes"

# 4. Sync with remote before pushing (safe option — merges any remote history)
git pull --rebase origin main

# 5. Push
git push origin main


java -jar packwiz-installer-bootstrap.jar https://raw.githubusercontent.com/ronank11/Private-Server-Modpack/main/pack.toml


packwiz modrinth add --project-id  --version-id 